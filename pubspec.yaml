name: portalempleado
description: Trabajo final ciclo

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dependency_overrides:
  intl: ^0.18.0
  flutter_localizations:
    sdk: flutter


  cupertino_icons: ^1.0.5
  firebase_core: ^2.13.0
  firebase_auth: ^4.6.1
  firebase_storage: ^11.2.1
  cloud_firestore: ^4.7.1
  file_picker: ^5.3.0
  shared_preferences: ^2.1.1
  table_calendar: ^3.0.9
  path_provider: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/

